This is a workout group planning application. It enables a group or one person to make a workout table together.

To run:
1. Create env file with key. Should be SECRET_KEY=some_random_secret_key

2. Run Database file once to create database -
python database.py

3. Install Packages -
pip install flask
pip install python-dotenv

4. Run main.py -
python main.py
